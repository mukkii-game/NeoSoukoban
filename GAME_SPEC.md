# GAME_SPEC.md — 『あなふさぎのよる』仕様書 (v1.5)

## コンセプト
プレイヤーは「はーん、倉庫番ね」と思って遊び始める。
しかし本当のルールは少しずつ違う。ゴールに見えるものは **穴** であり、
箱を「置く」のではなく穴を「塞ぐ」ゲームだった——さらに、壁も、おばけも、
穴そのものさえも「押せる」——と、遊びながら気づいていく **知識アンロック型パズル**。
10〜15分の短編。ひと晩（PM9:00〜夜明け）の物語。

- タイトル: **あなふさぎのよる**
- 主人公: 倉庫番のネコ 🐱
- 見た目: 絵文字 + CSS のタイニーなドット風。夜の倉庫。

## コアルール（真のルール）
1. グリッド上の完全ターン制。プレイヤーが1歩動く（or 待つ）と世界が1ターン進む。
2. **かべは、実は押せる**（1マス押し。見た目は普通の壁と完全に同じ）。
   - 非 wrap 面の外周のかべは「外側に立てない」ため幾何的に絶対動かない。
     これが実質的な盤の枠になる（枠は盤面の影で視認できる）。
   - `X` の **おせないかべ** だけは押せない。見た目はほんの少しだけ違う
     （わずかに暗く、かすかな鋲がある）。
3. **箱 📦**: 1マス押し。チェーン押し不可。
4. **おばけ 👻**:
   - 毎ターン、プレイヤーに1歩近づく（決定論的: 距離が大きい軸を先に、同値は横優先。
     塞がれていたらもう一方の軸。両方ダメなら停止）。
   - **プレイヤーのマスには入れない**（隣で止まる）。**このゲームに敗北は存在しない。**
   - **単体のおばけは押しても よける**: 1コマ押し出されてから、するりと元の位置へ
     戻る（「スカッ」演出）。よけるのに精一杯で、そのターンは動けない。
     押せるのは次の2つの場合だけ:
     (a) **真後ろが開いた穴のとき**（逃げ場がない → 落ちて栓になる）
     (b) **2体以上が縦横に列をなしているとき**（列ごとまとめて押せる。
         列の先が穴なら先頭の1体が落ちる）
   - つまり「並ばせる」「穴の前に追い込む」という位置取りがパズルになる。
     N 個の連続した穴を列だけで全部塞ぐには N+1 体の列が要る
     （塞いだ穴の上を列が渡るため、最後は単体になり押せなくなる）。
   - 押されたおばけは目を回し（💫）、そのターンは動けない。
   - おばけは開いた穴・壁・箱・他のおばけには入らない（自分からは穴に落ちない）。
5. **穴 🕳**: このゲームの「ゴール」。押せるもの（かべ・箱・おばけ）が入ると
   落ちて塞がる（消滅し、穴は床になる）。**すべての穴を塞ぐとクリア**。
   **開いた穴そのものも押して動かせる**（押し先が壁・オブジェクト・別の穴のときは
   動かず、プレイヤーは怖くて入れない 💦）。塞がった穴の上は歩ける。
6. **湧き**: 開いた穴は N ターンごとに 👻 を1体産む（レベル設定。0 = 湧かない）。
   湧く1ターン前に 👀 が出る。塞がれば湧かない。同時存在数は ghostCap で制限。
7. **ループ面**: `wrap: true` のレベルは盤面の上下左右がつながっている。
   プレイヤー・押されたもの・おばけの追跡すべてに適用（最短方向で追う）。
   ループ面では外周固定則が働かないため、静的壁はすべて `X` で書く。

## ターン順序（決定論）
1. プレイヤー入力（移動 / 押し / 穴押し / 待機）。
   **できない操作（壁ドン・押せない・よけられる・こわい）でも世界は1ターン進む**
   ＝壁にぶつかるのが実質の「待つ」。Space の明示的な待機も残してある（補助）。
2. 落下判定 → 全穴が塞がればその瞬間クリア。
3. おばけ移動（盤面の読み順 y→x で1体ずつ。目を回している個体はスキップ。
   プレイヤーのマスには入らない）。
4. 湧きカウンタ減算 → 0 の穴から 👻 生成（そのターンは動かない）。

## マップ記号
`#` かべ(押せる/外周は幾何的に不動) ・ `X` おせないかべ ・ `.` 床 ・
`P` プレイヤー ・ `B` 箱 ・ `G` おばけ ・ `O` 穴

## 知識アンロックの流れ
| # | 気づき | 教え方 |
|---|--------|--------|
| A | ゴールは穴で、入れた物は消える | L1 で自然に見せる |
| B | かべは押せる | L3: 箱がなく、本当に壁しかない密室。「ぐうぜん押してしまって気づく」導線 |
| C | 👻は単体では押せないが、真後ろが穴なら落とせる | L5: 箱が1つ足りない。よけられる体験→追い込む |
| D | 穴は湧き口。湧いた👻も栓 | L6: 目の前で穴から👻が湧く |
| E | あえて塞がず湧かせて自給 | L7: 箱ゼロ |
| F | 👻の列はまとめて押せる | L8: 1本道に並ばせて、3連穴へ押し込む（4体で3穴） |
| G | 世界の端はつながっている | L9: 中央のX壁で分断。外周壁なし。歩けば気づく |
| H | 穴そのものも押せる | L11: 箱が上段から出られない。穴を迎えに行かせる |
| I | 複数知識の応用 | L12〜L15: 紛れのある広い面。「敵も穴も試してダメ…あっ壁か！」/「先に穴を並べてから押す」 |

## レベル構成（17面 = ひと晩）
| # | 時刻 | 名前 | 内容 |
|---|------|------|------|
| 1 | PM 9:00 | いつものよる | 純倉庫番 |
| 2 | PM 10:00 | まわりみち | 倉庫番の基本: 縦に重なった2箱は押せない→横にずらして分解 |
| 3 | PM 11:00 | いきどまり | 【aha B】壁しかない2部屋(11x7)。ミスリード用の箱を左室に多数（どれも穴に届かない） |
| 4 | AM 0:00 | まよなかのおきゃく | 👻登場。ついてくるが何もできないと学ぶ |
| 5 | AM 0:30 | おばけのぎょうれつ | 【aha C】単体はよける→4体を1本道に並ばせ3連穴へ（列押しの学び） |
| 6 | AM 1:00 | じきゅうじそく | 【aha D】穴は湧き口。出てきた子を出てきた穴へ押し戻す（plopの学び） |
| 7 | AM 1:30 | わきでるよる | 湧きの応用: 箱1つ+穴3つ。湧きを栓に使う |
| 8 | AM 2:00 | ぐるりのはて | 【aha E】ループ面 16x10（大型化で端の存在を意識させない）。箱は中央寄り・紛れの👻 |
| 9 | AM 2:30 | おいかけっこ | ループ + 👻誘導（ループ越しの plop） |
| 10 | AM 3:00 | とどかないはこ | 【aha F】穴そのものを押して迎えに行く |
| 11 | AM 3:20 | はこがたりない | 応用: 箱1つ穴2つ。追い込み plop or 穴を動かして背後に置く |
| 12 | AM 3:40 | かべとおばけ | 応用: 敵も穴も試してダメ→壁が必須（wallPush 非可解証明つき）。11x9 |
| 13 | AM 4:00 | あふれるまえに | 応用: 多穴+湧き圧。外周にパックマン式ワープトンネル（上下・左右）＝逃げ道 |
| 14 | AM 4:20 | じゅんびのじかん | 応用: 穴を並べてから、ぎょうれつを押し込む複合。右ゾーンに引っ掛け柱 |
| 15 | AM 4:40 | だいぎょうれつ | 豪快枠: 16幅。5体の大行列を4連穴へ一気に押し込むドラムロール |
| 16 | AM 5:00 | おおそうじ | 豪快枠: 13x10。穴2つを押して4連ラインを御膳立て→5体を連れてきて大掃除 |
| 17 | よあけまえ | おわかれ | 最終面。序盤に右へ急行し右側上下の湧き口を👻で封じないと溢れて苦しくなる。最後は👻とのお別れ（エンディングへ） |

各レベルは `tools/solve.mjs` により機械的にクリア可能性を検証する。
aha レベル（3,5,7,8,10,11,12,14,15）は「その知識なしでは解けない」ことも検証する
（wallPush / ghostPush / spawnInterval / wrap / holePush の各ナイーブ化）。
n16 は敵5体の探索爆発で BFS 証明不能だが、箱ゼロ・押せる壁ゼロで栓＝おばけのみのため自明。

## レベルデザイン指針（囲まれ対策）
- おばけが複数いる広い平面には、**引っ掛け用のブロックを最低1つ**置く
  （柱に敵を引っ掛ければ、手数はかかるが逃げ道がある状態を保つ）。
  完全に詰んでも Undo/Retry/💡 で復帰できるが、「逃げられなくもない」余地を残す。

## 必須機能
- キーボード: 矢印/WASD 移動（ぶつかっても1ターン進む）、Space 待機（補助）、
  Z Undo、R リトライ、Esc レベル選択
- スマホ: スワイプ移動 + 画面下 D-pad（中央 = 待機）+ Undo/Retry ボタン
- タイトル / レベル選択（順次アンロック）/ クリア演出 / 短いエンディング
- 💡 模範解答の自動再生（ギブアップ）。解データは `node tools/solve.mjs --emit` が
  `solutions.js` に自動生成する（手で書かない）
- 🔓 検証用の全レベル開放トグル（レベル選択画面の下部）
- **行き詰まり検出**: 入力後 750ms アイドルで現在の状態から予算つき BFS（25,000状態）を
  実行。全探索し尽くして解が無ければ「詰み」を証明し 💭 で Z/R を提案する。
  予算切れ（大きい面）のときは何も言わない＝誤報を出さない。解ける状態に戻れば警告は消える
- 音: WebAudio のミニ SE + 夜のループ BGM（A→A→B 構成。プレイ中のみ）+ ミュート永続化
- 常時アニメ: ネコ/おばけ/箱のゆらぎは個体ごとに位相と速度をずらす（描画のみ乱数可）。
  押せる壁は動かない見た目を保つ（揺れたら正体がバレる）
- セーブ: クリア状況を localStorage に保存
- コンソールエラーなし。file:// 直開きで動作。GitHub Pages 対応（index.html）

## 非採用・保留（意図的な判断）
- タイム・ランク・コンボ等のアクション要素（思考の邪魔）
- 敗北状態（「触ると即死」体験になるため v1.3 で全廃）
- おばけの「疲れて止まる」スタミナ制: 面白いが状態空間が疲労カウンタ倍に膨らみ、
  全レベル機械検証（このゲームの品質保証の要）が破綻するため保留。
  導入するならカウンタ周期を極小にして専用レベルでのみ検証する設計が必要
- 盤外（UI説明文の中の絵文字）を押せるメタ仕掛け: エンジン純度と検証体制を壊さず
  実装する設計（盤内に「偽UI行」を持つ専用レベル）を将来課題として保留
